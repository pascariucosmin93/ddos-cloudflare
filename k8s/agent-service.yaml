apiVersion: v1
kind: Service
metadata:
  name: ddos-agent
  namespace: ddos-protection
spec:
  selector:
    app: ddos-agent
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
